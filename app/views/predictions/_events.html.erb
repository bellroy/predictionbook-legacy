<ul id="responses">
  <%- unless events.blank? -%>
    <%- events.each do |event| -%>
    <li class="<%= event.is_a?(Response) ? 'response' : 'change' %>">
      <%= render_event_partial(event) %>
    </li>
    <%- end -%>
  <%- end -%>  
</ul>
